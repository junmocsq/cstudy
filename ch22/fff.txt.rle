#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(void)
{
 FILE *tmptr = tmpfile();
 fclose(tmptr);
 / remove(tmptr);
 fprintf(stder, "junmocsq er:%s\n", "m");
 printf("%a\n", 17.625);
 char abc[10];
 FILE *f1 = fopen("f1.txt","r+");
 FILE *f2 = fopen("f1.txt","w+");
 
 while (!feof(f1)
 {
 fgets(abc,10,f1);
 fputs(abc,f2);
 }
 
 / char abc[10];
 / fgets(abc, 10, stdin);
 / printf("%s %c\n", abc, abc[9]);
 fclose(f1);
 fclose(f2);
 return 0;
}